<%= form_for(@viewedpro) do |f| %>
  <% if @viewedpro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@viewedpro.errors.count, "error") %> prohibited this viewedpro from being saved:</h2>

      <ul>
      <% @viewedpro.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
